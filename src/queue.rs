//! Playback queue management.

/// Track queue structure for managing playback order.
#[derive(Debug, Default)]
pub struct Queue;

impl Queue {
    /// Creates a new `Queue`.
    pub fn new() -> Self {
        Self
    }
}
